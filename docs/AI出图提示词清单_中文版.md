# 摄政王的游戏 — AI 出图提示词清单（中文版）

> 英文原版见同目录 `AI出图提示词清单.md`。本文件是**中文提示词版**：每个资产的【文件名 / 分辨率 / 代码位置】与英文版一致，仅把实际出图提示词译为中文。
> 适用：支持中文的生图模型（如可理解中文的扩散模型）。若你用的是只吃英文的模型，建议用英文原版效果更好。
> 已确认决策：卡牌用 **Tier1（4 档 rank 底图）**，配色统一到**调色板 A（手牌暗灰）**。

---

## 0. 通用使用说明

- **所有提示词共用前缀**（见 §1），可复制拼接。
- **分辨率约定**：卡牌/立绘 `512×768`（2:3 竖）；图标 `256×256`；边框/按钮 `512×512`（九宫格，四角留 64px 不可拉伸花纹，中间纯色可拉伸）；整幅背景 `1920×1080`。
- **透明背景**：图标、边框、按钮、星标类一律要求「透明背景」；卡牌底图可保留暗色底（不透明）或透明由代码叠。
- **版权**：本项目已因版权从《苏丹的游戏》改名为《摄政王的游戏》，**禁止复制任何原版美术**，提示词已加负向约束。
- **代码位置**：英文版配套计划 `.workbuddy/plans/cosmic-vortex-turing.md`（该计划文件未被提交、已丢失，出图时按本表文件名归类即可）。

---

## 1. 全局风格与配色（所有提示词的前缀 + 负向）

**中文风格前缀（拼到每个提示词前面，占位符 `${中文前缀}`）：**
```
华丽暗黑奇幻宫廷游戏UI素材，镀金金边装饰（颜色 #c8a84e），做旧深色木料与羊皮纸纹理，氛围感朦胧光影，高度精细，干净抗锯齿边缘，
```

**中文负向约束（加在结尾，占位符 `${中文负向}`）：**
```
不抄袭任何现有商业游戏美术，无文字，无字母，无UI控件，无水印，无签名，
```

**调色板 A（卡牌/边框底色，已统一）：**
| rank | 名称 | 十六进制 | 描述 |
|---|---|---|---|
| STONE | 岩石 | `#26211C` | 暗岩石灰黑，粗糙石面 |
| BRONZE | 青铜 | `#21291A` | 暗铜绿，铜锈 |
| SILVER | 白银 | `#1F2126` | 暗银蓝，冷调 |
| GOLD | 黄金 | `#292414` | 暗金棕，金箔 |

全局金：`GOLD #c8a84e` / 亮金 `#e8d48b` / 暗金 `#8a6820`；文字米色 `#f0e6c8`；类型色：欢愉品红 `#8b3a5c`、奢靡蓝 `#3a5c8b`、征伐绿 `#3a5b3a`、杀戮暗红 `#6b2a2a`。

---

## 2. 主菜单（`scripts/ui/MainMenu.gd`）

| 文件名 | 分辨率 | 代码位置 | 中文提示词 |
|---|---|---|---|
| `ui/main_menu_bg.png` | 1920×1080 | `:31` | `${中文前缀} 全屏背景，用于奇幻宫廷游戏主菜单，昏暗王座大厅剪影，拱门与垂挂旗帜隐于阴影，边缘暗角，深黑底色配金 #c8a84e 点缀，中央留白放文字，电影感，${中文负向}` |
| `ui/title_emblem.png` | 512×256 透明 | `:41` | `${中文前缀} 对称王室纹章徽记，华丽金色花丝，摄政王王冠母题，深色背景，透明，logo装饰，${中文负向}` |
| `ui/menu_button.png` | 512×512 九宫格 | `:63-82` | `${中文前缀} 华丽金边按钮面板，深色羊皮纸中心，斜面镀金边框 #c8a84e，透明中心，九宫格安全边，${中文负向}` |

---

## 3. UI 框架 / 边框（全部 `StyleBoxFlat` → `StyleBoxTexture`）

| 文件名 | 分辨率 | 代码位置 | 中文提示词 |
|---|---|---|---|
| `ui/statusbar_bg.png` | 512×128 九宫格 | `StatusBar.gd:30-40` | `${中文前缀} 细长的顶部状态栏条带，深色木料配微妙金色 #c8a84e 下边线，透明，${中文负向}` |
| `ui/sorceress_panel.png` | 512×512 九宫格 | `SorceressScene.gd:94-103` | `${中文前缀} 大型华丽弹窗外框，深色羊皮纸内里，厚重镀金 #c8a84e 边框带角部花饰，投影，透明中心，${中文负向}` |
| `ui/sorceress_portrait_frame.png` | 512×768 透明 | `SorceressScene.gd:114-122` | `${中文前缀} 竖式肖像外框，淡紫调(#8b3a5c)半透明华丽边框，奇幻女术士主题，透明中心，${中文负向}` |
| `ui/cardbox_frame.png` | 512×512 九宫格 | `CardBox.gd:16-25` | `${中文前缀} 木质宝箱/卷轴盒外框，深色木料配金色 #c8a84e 花丝，神秘感，透明中心，${中文负向}` |
| `ui/cardbox_card_frame.png` | 512×768 九宫格 | `CardBox.gd:76-101` | `${中文前缀} 居中大卡牌展示外框，华丽金色 #c8a84e 边框，深色内里，透明中心，${中文负向}` |
| `ui/map_board.png` | 1920×1080 | `MapRitePanel.gd:24-38` | `${中文前缀} 奇幻城市地图底板背景，做旧羊皮纸配隐约城区轮廓，金色 #c8a84e 海岸线，暗沉氛围，${中文负向}` |
| `ui/rite_popup_frame.png` | 512×512 九宫格 | `RiteDetailPopup.gd:70-90` | `${中文前缀} 仪式配置弹窗外框，双栏提示，深色羊皮纸，金 #c8a84e 边框，透明中心，${中文负向}` |
| `ui/rite_panel_inner.png` | 512×256 九宫格 | `RiteDetailPopup.gd:102-116,230-244` | `${中文前缀} 内层子面板外框，细金 #8a6820 边框，深色，透明中心，${中文负向}` |
| `ui/settlement_frame.png` | 512×512 九宫格 | `SettlementScreen.gd:92-116` | `${中文前缀} 结算界面外框，深色配金 #c8a84e 边框，华丽，透明中心，${中文负向}` |
| `ui/settlement_dice_tray.png` | 256×256 九宫格 | `SettlementScreen.gd:92-116` | `${中文前缀} 骰盘面板，深色凹陷区域，细金边框，透明中心，${中文负向}` |
| `ui/popup_frame_generic.png` | 512×512 九宫格 | `PopupManager.gd` | `${中文前缀} 通用小型弹窗外框，深色羊皮纸，金 #c8a84e 边框，透明中心，${中文负向}` |
| `ui/cardzone_border.png` | 512×128 九宫格 | `MainScene.gd:391-401` | `${中文前缀} 横向手牌区分隔边框，金 #8a6820 细线，深色，透明，${中文负向}` |
| `ui/insight_bubble.png` | 256×128 九宫格 | `InsightController.gd:128-140` | `${中文前缀} 小型对话气泡外框，深色配细金 #8a6820 边框，透明中心，${中文负向}` |

---

## 4. 按钮（5 家族，共用少量纹理 + 三态）

| 文件名 | 分辨率 | 代码位置 | 中文提示词 |
|---|---|---|---|
| `ui/btn_gold_normal.png` | 512×512 九宫格 | 家族 A（`MainScene.gd:101-114` 默认主题）| `${中文前缀} 金边按钮，常态，深色羊皮纸中心 #2a1c0a，斜面镀金边框 #8a6820，透明中心，${中文负向}` |
| `ui/btn_gold_hover.png` | 512×512 九宫格 | 同上 hover | `${中文前缀} 金边按钮，悬停态，更亮羊皮纸 #3a2c0a，发光金 #e8d48b 边框，透明中心，${中文负向}` |
| `ui/btn_gold_pressed.png` | 512×512 九宫格 | 同上 pressed | `${中文前缀} 金边按钮，按下态，下陷深色中心，强烈金 #e8d48b 内发光，透明中心，${中文负向}` |
| `ui/btn_menu_large.png` | 512×256 九宫格 | 家族 B（`MainMenu.gd:63-82`）| `${中文前缀} 大型菜单按钮，240x48 观感，华丽金 #c8a84e 边框，深色中心，透明，${中文负向}` |
| `ui/btn_sorceress.png` | 512×128 九宫格 | 家族 D（`SorceressScene.gd:517-543`）| `${中文前缀} 细长对话按钮，金 #8a6820 边框，深色中心 #2a1810，透明，${中文负向}` |
| `ui/btn_map_node.png` | 512×128 九宫格 | 家族 E（`MapRitePanel.gd:159-172`）| `${中文前缀} 仅底部金色下划线按钮，深色，细金 #8a6820 底边，透明，${中文负向}` |

---

## 5. 图标（全部 `256×256` 透明 PNG，替换 emoji）

| 文件名 | 含义 | 代码位置 | 中文提示词 |
|---|---|---|---|
| `icons/type_lust.png` | 欢愉 | `RiteSlotDrop.gd:27` 等 | `${中文前缀} 小图标：圣杯含心，品红 #8b3a5c 微光，奇幻，透明，${中文负向}` |
| `icons/type_luxury.png` | 奢靡 | 同上 | `${中文前缀} 小图标：嵌宝石金币堆，蓝 #3a5c8b 微光，奇幻，透明，${中文负向}` |
| `icons/type_conquest.png` | 征伐 | 同上 | `${中文前缀} 小图标：交叉剑/旗帜，绿 #3a5b3a 微光，奇幻，透明，${中文负向}` |
| `icons/type_murder.png` | 杀戮 | 同上 | `${中文前缀} 小图标：滴血匕首，暗红 #6b2a2a 微光，奇幻，透明，${中文负向}` |
| `icons/attr_body.png` | 体魄 | `MainScene.gd:18` 等 | `${中文前缀} 小图标：肌肉手臂/盾，纯金，透明，${中文负向}` |
| `icons/attr_combat.png` | 战斗 | 同上 | `${中文前缀} 小图标：剑，纯金，透明，${中文负向}` |
| `icons/attr_survival.png` | 生存 | 同上 | `${中文前缀} 小图标：叶+火，纯金，透明，${中文负向}` |
| `icons/attr_social.png` | 社交 | 同上 | `${中文前缀} 小图标：双连环，纯金，透明，${中文负向}` |
| `icons/attr_charm.png` | 魅力 | 同上 | `${中文前缀} 小图标：镜/玫瑰，纯金，透明，${中文负向}` |
| `icons/attr_stealth.png` | 隐匿 | 同上 | `${中文前缀} 小图标：兜帽/阴影，纯金，透明，${中文负向}` |
| `icons/attr_wisdom.png` | 智慧 | 同上 | `${中文前缀} 小图标：翻开的书/眼，纯金，透明，${中文负向}` |
| `icons/attr_magic.png` | 魔力 | 同上 | `${中文前缀} 小图标：火花/符文，纯金，透明，${中文负向}` |
| `icons/res_gold.png` | 金币 | `ResourceCardManager.gd` 等 | `${中文前缀} 小图标：带摄政王纹章的金币，#c8a84e，透明，${中文负向}` |
| `icons/item.png` | 物品 | `RiteSlotDrop.gd` | `${中文前缀} 小图标：放大镜罩住小物件，纯金，透明，${中文负向}` |
| `icons/intel_insight.png` | 洞察 | `ResourceCardManager.gd:158-169` | `${中文前缀} 小图标：睁开的眼，纯金，透明，${中文负向}` |
| `icons/intel_secret.png` | 秘氛 | 同上 | `${中文前缀} 小图标：戴面具的脸，纯金，透明，${中文负向}` |
| `icons/intel_chance.png` | 机遇 | 同上 | `${中文前缀} 小图标：四叶草/骰子，纯金，透明，${中文负向}` |
| `icons/intel_inside.png` | 内幕 | 同上 | `${中文前缀} 小图标：门边的耳朵，纯金，透明，${中文负向}` |
| `icons/intel_omen.png` | 预兆 | 同上 | `${中文前缀} 小图标：彗星，纯金，透明，${中文负向}` |
| `icons/intel_cult.png` | 密教 | 同上 | `${中文前缀} 小图标：神秘符号，纯金，透明，${中文负向}` |
| `icons/rep_fame.png` | 名望 | `StatusBar.gd:54-60` | `${中文前缀} 小徽章：桂冠，绿 #5a9a5a，透明，${中文负向}` |
| `icons/rep_infamy.png` | 恶名 | 同上 | `${中文前缀} 小徽章：破碎盾，红 #aa3030，透明，${中文负向}` |
| `icons/rep_power.png` | 权势 | 同上 | `${中文前缀} 小徽章：王冠，紫 #9a6aba，透明，${中文负向}` |
| `icons/rep_chivalry.png` | 义名 | 同上 | `${中文前缀} 小徽章：初升太阳，蓝 #5a8aba，透明，${中文负向}` |
| `icons/rep_occult.png` | 灵知 | 同上 | `${中文前缀} 小徽章：三角中的眼，绿 #6a8a5a，透明，${中文负向}` |
| `icons/dice_gold.png` | 金骰 | `StatusBar.gd:88` | `${中文前缀} 小图标：单颗带金点的骰子，#c8a84e，透明，${中文负向}` |
| `icons/book.png` | 书籍 | `CardFactory.gd:170` | `${中文前缀} 小图标：带锁扣的合起书本，纯金，透明，${中文负向}` |
| `icons/skull_insight.png` | 俺寻思 | `MainScene.gd:521` | `${中文前缀} 小图标：程式化骷髅带思考火花，深紫，透明，${中文负向}` |
| `icons/star.png` | 品级星 | `RG` 多处 | `${中文前缀} 小五角星，金 #e8d48b，透明，${中文负向}`（代码按 rank 复制 1~4 颗）|
| `icons/success.png` | 成功 | `SettlementScreen.gd` 等 | `${中文前缀} 小对勾印章，绿，透明，${中文负向}` |
| `icons/fail.png` | 失败 | 同上 | `${中文前缀} 小叉/X 印章，红，透明，${中文负向}` |
| `icons/home.png` | 返回主菜单 | `PopupManager.gd:151` | `${中文前缀} 小图标：屋顶/家，纯金，透明，${中文负向}` |
| `icons/close_x.png` | 关闭 | 多处 | `${中文前缀} 小华丽 X，纯金，透明，${中文负向}` |
| `icons/reroll.png` | 重投 | `SettlementScreen.gd:173` | `${中文前缀} 小图标：双环形箭头，纯金，透明，${中文负向}` |

---

## 6. 立绘 / 肖像（最高优先替换）

| 文件名 | 分辨率 | 代码位置 | 中文提示词 |
|---|---|---|---|
| `portraits/sorceress.png` | 512×768 | `SorceressScene.gd:130-135` | `${中文前缀} 全身像，神秘奇幻女术士，华丽长袍配品红 #8b3a5c 与金 #c8a84e 镶边，神秘光环，深色背景，精细面容，高贵，${中文负向}` |
| `portraits/player.png` | 512×768 | `CardFactory.gd:25-78` | `${中文前缀} 中立贵族摄政王（玩家头像）肖像，深色朝服配金 #c8a84e 镶边，纯色背景，${中文负向}` |
| `portraits/meji.png` | 512×768 | 同上 | `${中文前缀} 优雅女廷臣肖像，奇幻，深色背景，金边礼裙，${中文负向}` |
| `portraits/zhaqiyi.png` | 512×768 | 同上 | `${中文前缀} 严厉军官肖像，奇幻盔甲配金边，深色背景，${中文负向}` |
| `portraits/tietou.png` | 512×768 | 同上 | `${中文前缀} 魁梧忠诚卫士肖像，奇幻，深色背景，简甲，${中文负向}` |
| `portraits/kuaijiao.png` | 512×768 | 同上 | `${中文前缀} 机敏谋士/顾问肖像，奇幻长袍，深色背景，金边，${中文负向}` |

---

## 7. 卡牌稀有度背景（核心，Tier1，调色板 A）

> 4 档底图，所有卡（摄政王令/角色/书）按 `rank` 复用同一张。边框亮度随品质提升：石→暗金，金→亮金 `#e8d48b`。类型强调色由代码叠加，底图不区分类型。

| 文件名 | rank | 分辨率 | 代码位置 | 中文提示词 |
|---|---|---|---|---|
| `cards/ranks/stone.png` | 岩石 STONE | 512×768 | `CardFactory.gd` / `RiteSlotDrop.gd` / `CardBox.gd` / `SorceressScene.gd` | `${中文前缀} 卡牌底图，暗粗糙石面 颜色 #26211C，雕裂缝理，微妙暗金 #8a6820 边框，上下华丽石框，中心留白，竖式2:3，${中文负向}` |
| `cards/ranks/bronze.png` | 青铜 BRONZE | 512×768 | 同上 | `${中文前缀} 卡牌底图，暗铜绿锈面 颜色 #21291A，铜绿纹理，细金 #8a6820 边框，上下华丽框，中心留白，竖式2:3，${中文负向}` |
| `cards/ranks/silver.png` | 白银 SILVER | 512×768 | 同上 | `${中文前缀} 卡牌底图，暗冷银蓝面 颜色 #1F2126，拉丝金属光泽，优雅银 #aab4c8 边框，上下华丽框，中心留白，竖式2:3，${中文负向}` |
| `cards/ranks/gold.png` | 黄金 GOLD | 512×768 | 同上 | `${中文前缀} 卡牌底图，暗富金棕面 颜色 #292414，金箔纹理，亮金 #e8d48b 华丽花丝边框，上下奢华框，中心留白，竖式2:3，${中文负向}` |

> 可选进阶（Tier2，本次不做）：`cards/sultan/<type>_<rank>.png` 共 16 张，在以上底图上加整幅类型插画（欢愉=享乐场景/奢靡=挥霍场景/征伐=战场/杀戮=暗杀），边框沿用对应 rank。

---

## 8. 地图 / 仪式节点

| 文件名 | 分辨率 | 代码位置 | 中文提示词 |
|---|---|---|---|
| `ui/map_board.png` | 见 §3 | `MapRitePanel.gd:24-38` | 已列 §3 |
| `ui/btn_map_node.png` | 见 §4 | `MapRitePanel.gd:159-172` | 已列 §4 家族 E |

---

## 9. 出图后接入代码的简要指引（待你决定实施时参考）

1. 建目录 `res://assets/images/{ui,icons,portraits,cards/ranks}`。
2. 新增 AutoLoad `scripts/autoload/ArtRegistry.gd`，按 `rank` 映射 4 张底图。
3. 卡牌渲染点（`CardFactory` 4 个 `make_*`、`CardBox.show_card_display`、`RiteSlotDrop` 两处、`SorceressScene._show_swap_card`）把 `StyleBoxFlat.bg_color = SC.get(rank)` 改为 `StyleBoxTexture(bg_texture=ArtRegistry.rank_bg(rank))`。
4. emoji `Label` 处改为 `TextureRect`（图标）/`TextureRect` 立绘，纹理走 `preload` 或 `ArtRegistry` 映射。
5. 边框/按钮 `StyleBoxFlat` 改为 `StyleBoxTexture`（九宫格，设 `region_rect` 与 `expand_margin_left/top/right/bottom`，**注意 Godot 4 用 `expand_margin_*` 而非 Godot 3 的 `margin_*`**）。
